	<% if !app.icon.blank? %>
		<%= link_to image_tag app.icon.thumb('128x128#').url, app_path(app) %>
  <% else %>
 		<%= link_to image_tag('apps/'+app.app_type.name+'.png'), app_path(app) %>
 	<% end %>