<% if signed_in? %>
	<% if current_user.admin? %>
		<%= render 'shared/home_admin' %>
	<% else %>
  	<%= render 'shared/home_authenticated' %>
  <% end %>
<% else %>
  <%= render 'shared/home_guest' %>
<% end %> 