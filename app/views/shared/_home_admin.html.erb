<% provide(:title, 'Users') %>

<h1>Users</h1>

<%= will_paginate @users %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate @users %>

<div class="add-link">
	<%= link_to "Add User", new_user_path %>
</div>